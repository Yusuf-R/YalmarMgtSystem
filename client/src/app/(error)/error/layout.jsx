// components/ErrorLayout.jsx
import React from "react";

function ErrorLayout({ children }) {
    return (
            <section>
                {children}
            </section>
    );
}

export default ErrorLayout;
